
- grep -rl TODO *
- Bug: If Yanel is behind a proxy and the yanel prefix is being used instead ROOT, then something is wrong with session handling ...
- Add WebDAV and APP
- Downgrading/Upgrading, automatic updates, history, Maven dependency ...
- Security: Policies of Realms should NOT be inherited resp. configurable
- Checking of well-formedness during saving does not work if being offline
-- This seems to be because of the DOCTYPE specified within the XML documents ... (try to resolve local)
- Make authorization and authentication implementation configurable per realm
- Implement Admin interface with listing of repositories/realms and mountpoints
- Implement Locking
- YanelServlet: if realm has no authorization specified then the ROOT one is being taken instead that access is being denied: Makes this configurable
- YanelServlet: Declare Realm for authorization on authentication screen
- YanelServlet: Unify authentication scheme
- YanelServlet: Improve exception handling (also handle <yanel ... as an XML object)
- Implement authentication
- ResourceTypeRegistry class loading needs to be improved (see Yarep how it should be done ...)
- Change rti map to XML with the possibility of resource specific path config
- Introduce Map/Realm mounting
- Introduce chain of responsibility for MapImpl (e.g. javadoc)
-- Would it make sense to put this also into the repository layer?
- Build resource implementations dynamically (based on configuration): ResourceRegistry ...
- Add yanel version (such that for instance the webapp can display the version, e.g. 0.0.1-dev-r13812)
- Make Tomcat targets configurable during configure.sh
- Specify versions of implementations explicitely and make them downloadable dynamically based on the registered resource types
- Add Windows bat files
- Add servlet engine, e.g. Jetty or Tomcat
- Replace most System.err/out by log4j
- Import any website with Nutch Crawler and rti map creator
- Implement Distribution target (zip and tar.gz)
- Move runtime lib dependencies into init-runtime target
