<?xml version="1.0"?>

<!-- NOTE: Do NOT edit this file, but rather copy this file to local/local.yanel.xml and make changes inside this local copy -->

<yanel xmlns="http://www.wyona.org/yanel/1.0" version="2012012400">

  <changes>
    <change version="20150330">Generic mail.smtp properties enabled, like for example mail.smtp.localhost</change>
    <change version="20120124">target environment configuration added</change>
    <change version="20100126">smtp configuration added</change>
  </changes>

  <!-- # Yanel version: Will be added during the build process (see src/build/(local.)build.properties and src/build/yanel2properties.xsl) -->
<!--
  <version revision="REVISION" version="VERSION"/>
-->
  
  <!-- # Realms configuration, also see http://www.yanel.org/en/documentation/configuration/realms_xml.html -->
  <realms-config src="realms.xml"/>
  
  <!-- # ResourceTypes configuration -->
  <resource-types-config src="resource-types.xml"/>
  
  <!-- # Reserved-Prefix configuration -->
  <reserved-prefix>yanel</reserved-prefix>
  
  <!-- # Scheduler configuration -->
  <scheduler enabled="false"/>
  
  <!-- # Mail server configuration.
         Username and password attributes are optional,
         but mail-servers require authentication in order to relay messages to other hosts/domains
  -->
<!--
  <smtp host="mail.foo.bar" port="25" username="foo" password="bar"/>
-->
<!-- INFO: About mail.smtp properties see for example https://javamail.java.net/nonav/docs/api/com/sun/mail/smtp/package-summary.html
  <smtp host="localhost" port="25" mail.smtp.localhost="www.foo.bar"/>
-->

  <!-- # Target environment configuration (Please note that one is free to choose the name of a target environment, but it's recommended to use the below names by convention: prod = production, ua-test = user acceptance test, ci-test = continuous integration test, dev = development) -->
<!--
  <target-environment>prod</target-environment>
  <target-environment>ua-test</target-environment>
  <target-environment>ci-test</target-environment>
  <target-environment>dev</target-environment>
-->

<!-- # Optional trust-store configuration. See http://www.yanel.org/en/documentation/security/ssl.html for more details -->
<!--
  <trust-store src="/foo/bar/truststore.jks" password="MY_PASSWORD"/>
-->

</yanel>
