Only the interfaces within this api directory should be implemented.

If a versioned class has once been released, then it cannot be changed anymore (not even enhanced).
A new version needs to be created.
